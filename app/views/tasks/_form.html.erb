<div class="task_form">
<%= form_for @task do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :body %>
  <%= t.text_area :body %>
  <%= t.label :rank %>
  <%= t.text_field :rank %>
  <%= t.submit %>
<% end %>
</div>
